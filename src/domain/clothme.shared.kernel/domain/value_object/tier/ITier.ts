export default interface ITier {
    // GetTier ()
    /*
    IsTierActive (CompanyId) : boolean // checks company tier
    Renew (CompanyId);
    Cancel (CompanyId);
    UpGrade (CompanyId);
    DownGrade (CompanyId);
    */
}